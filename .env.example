# ─── PRIMARY: Google Gemini (FREE — no credit card needed) ──────────────
# Get your key at https://aistudio.google.com/apikey
# Free tier: 60 requests/minute, ~1500 requests/day
GEMINI_API_KEY=your-gemini-api-key-here

# Model overrides (optional — defaults shown)
GEMINI_MODEL_DEEP=gemini-1.5-pro        # used when tier = "deep"
GEMINI_MODEL_QUICK=gemini-1.5-flash     # used when tier = "quick"

# ─── FALLBACK: Anthropic Claude Haiku ────────────────────────────────────
# Get key at https://console.anthropic.com
# Used ONLY when Gemini returns 429/500/quota errors — invisible to users.
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Claude model (optional — default shown)
CLAUDE_MODEL=claude-3-haiku-20240307

# ─── Server ─────────────────────────────────────────────────────────────
# Railway sets PORT automatically. Override only for local dev.
PORT=3000
